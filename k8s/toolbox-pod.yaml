apiVersion: v1
kind: Pod
metadata:
  name: toolbox
  namespace: checkout
spec:
  containers:
    - name: toolbox
      image: curlimages/curl:8.7.1
      command: ["sh", "-c", "sleep 3600"]
  restartPolicy: Never